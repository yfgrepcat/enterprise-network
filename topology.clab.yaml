# Host bridges must exist before deploy:
#     sudo ip link add name net-isp   type bridge && sudo ip link set net-isp   up
#     sudo ip link add name net-nomad type bridge && sudo ip link set net-nomad up
#     sudo ip link add name net-site  type bridge && sudo ip link set net-site  up
#
# Addressing
#   Loopbacks:  10.0.0.{1..4}   (P1..P4)
#               10.0.0.{21..23} (PE-isp, PE-nomad, PE-site)
#   Core P2P:   10.1.<lo><hi>.0/31  (e.g. P1-P2 = 10.1.12.0/31)
#   PE uplinks: 10.2.<n>.0/31       (P1-PEisp = 10.2.1.0/31, etc.)

name: enterprise-ospf-bgp

mgmt:
  network: clab-mgmt
  ipv4-subnet: 172.20.20.0/24

topology:
  kinds:
    nokia_srlinux:
      image: ghcr.io/nokia/srlinux:24.10.7
      type: ixr-d2
    linux:
      image: ubuntu:22.04

  nodes:
    P1:
      kind: nokia_srlinux
      mgmt-ipv4: 172.20.20.11
      group: core
      startup-config: |
        set / interface ethernet-1/1 admin-state enable
        set / interface ethernet-1/1 subinterface 0 admin-state enable
        set / interface ethernet-1/1 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/1 subinterface 0 ipv4 address 10.1.12.0/31
        set / interface ethernet-1/2 admin-state enable
        set / interface ethernet-1/2 subinterface 0 admin-state enable
        set / interface ethernet-1/2 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/2 subinterface 0 ipv4 address 10.1.13.0/31
        set / interface ethernet-1/3 admin-state enable
        set / interface ethernet-1/3 subinterface 0 admin-state enable
        set / interface ethernet-1/3 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/3 subinterface 0 ipv4 address 10.1.14.0/31
        set / interface ethernet-1/10 admin-state enable
        set / interface ethernet-1/10 subinterface 0 admin-state enable
        set / interface ethernet-1/10 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/10 subinterface 0 ipv4 address 10.2.1.0/31
        set / interface ethernet-1/11 admin-state enable
        set / interface ethernet-1/11 subinterface 0 admin-state enable
        set / interface ethernet-1/11 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/11 subinterface 0 ipv4 address 120.0.164.1/24

    P2:
      kind: nokia_srlinux
      mgmt-ipv4: 172.20.20.12
      group: core
      startup-config: |
        set / interface ethernet-1/1 admin-state enable
        set / interface ethernet-1/1 subinterface 0 admin-state enable
        set / interface ethernet-1/1 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/1 subinterface 0 ipv4 address 10.1.12.1/31
        set / interface ethernet-1/2 admin-state enable
        set / interface ethernet-1/2 subinterface 0 admin-state enable
        set / interface ethernet-1/2 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/2 subinterface 0 ipv4 address 10.1.23.0/31
        set / interface ethernet-1/3 admin-state enable
        set / interface ethernet-1/3 subinterface 0 admin-state enable
        set / interface ethernet-1/3 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/3 subinterface 0 ipv4 address 10.1.24.0/31
        set / interface ethernet-1/10 admin-state enable
        set / interface ethernet-1/10 subinterface 0 admin-state enable
        set / interface ethernet-1/10 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/10 subinterface 0 ipv4 address 10.2.2.0/31

    P3:
      kind: nokia_srlinux
      mgmt-ipv4: 172.20.20.13
      group: core
      startup-config: |
        set / interface ethernet-1/1 admin-state enable
        set / interface ethernet-1/1 subinterface 0 admin-state enable
        set / interface ethernet-1/1 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/1 subinterface 0 ipv4 address 10.1.13.1/31
        set / interface ethernet-1/2 admin-state enable
        set / interface ethernet-1/2 subinterface 0 admin-state enable
        set / interface ethernet-1/2 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/2 subinterface 0 ipv4 address 10.1.23.1/31
        set / interface ethernet-1/3 admin-state enable
        set / interface ethernet-1/3 subinterface 0 admin-state enable
        set / interface ethernet-1/3 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/3 subinterface 0 ipv4 address 10.1.34.0/31
        set / interface ethernet-1/10 admin-state enable
        set / interface ethernet-1/10 subinterface 0 admin-state enable
        set / interface ethernet-1/10 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/10 subinterface 0 ipv4 address 10.2.3.0/31

    P4:
      kind: nokia_srlinux
      mgmt-ipv4: 172.20.20.14
      group: core
      startup-config: |
        set / interface ethernet-1/1 admin-state enable
        set / interface ethernet-1/1 subinterface 0 admin-state enable
        set / interface ethernet-1/1 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/1 subinterface 0 ipv4 address 10.1.14.1/31
        set / interface ethernet-1/2 admin-state enable
        set / interface ethernet-1/2 subinterface 0 admin-state enable
        set / interface ethernet-1/2 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/2 subinterface 0 ipv4 address 10.1.24.1/31
        set / interface ethernet-1/3 admin-state enable
        set / interface ethernet-1/3 subinterface 0 admin-state enable
        set / interface ethernet-1/3 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/3 subinterface 0 ipv4 address 10.1.34.1/31

    PE-isp:
      kind: nokia_srlinux
      mgmt-ipv4: 172.20.20.21
      group: edge
      startup-config: |
        set / interface ethernet-1/1 admin-state enable
        set / interface ethernet-1/1 subinterface 0 admin-state enable
        set / interface ethernet-1/1 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/1 subinterface 0 ipv4 address 10.2.1.1/31

    PE-nomad:
      kind: nokia_srlinux
      mgmt-ipv4: 172.20.20.22
      group: edge
      startup-config: |
        set / interface ethernet-1/1 admin-state enable
        set / interface ethernet-1/1 subinterface 0 admin-state enable
        set / interface ethernet-1/1 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/1 subinterface 0 ipv4 address 10.2.2.1/31

    PE-site:
      kind: nokia_srlinux
      mgmt-ipv4: 172.20.20.23
      group: edge
      startup-config: |
        set / interface ethernet-1/1 admin-state enable
        set / interface ethernet-1/1 subinterface 0 admin-state enable
        set / interface ethernet-1/1 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/1 subinterface 0 ipv4 address 10.2.3.1/31

    DHCP1:
      kind: linux
      binds:
        - ./dhcp:/opt/dhcp:ro
      cmd: /bin/bash -lc "bash /opt/dhcp/setup-dhcp.sh"

    TESTCLIENT:
      kind: linux
      image: ubuntu:22.04
      cmd: /bin/bash -lc "sleep infinity"
      exec:
        - bash -lc "DEBIAN_FRONTEND=noninteractive apt-get update && DEBIAN_FRONTEND=noninteractive apt-get install -y isc-dhcp-client iproute2 && dhclient -v eth1"

    # Host bridges
    net-isp:
      kind: bridge
    net-nomad:
      kind: bridge
    net-site:
      kind: bridge
    svc-net:
      kind: bridge

  links:
    # IGP Full Mesh Core
    - endpoints: ["P1:e1-1", "P2:e1-1"]   # 10.1.12.0/31
    - endpoints: ["P1:e1-2", "P3:e1-1"]   # 10.1.13.0/31
    - endpoints: ["P1:e1-3", "P4:e1-1"]   # 10.1.14.0/31
    - endpoints: ["P2:e1-2", "P3:e1-2"]   # 10.1.23.0/31
    - endpoints: ["P2:e1-3", "P4:e1-2"]   # 10.1.24.0/31
    - endpoints: ["P3:e1-3", "P4:e1-3"]   # 10.1.34.0/31

    # PE uplinks
    - endpoints: ["P1:e1-10", "PE-isp:e1-1"]    # 10.2.1.0/31
    - endpoints: ["P2:e1-10", "PE-nomad:e1-1"]  # 10.2.2.0/31
    - endpoints: ["P3:e1-10", "PE-site:e1-1"]   # 10.2.3.0/31

    # DHCP service segment
    - endpoints: ["P1:e1-11", "svc-net:p1-port"]
    - endpoints: ["DHCP1:eth1", "svc-net:dhcp-port"]
    - endpoints: ["TESTCLIENT:eth1", "svc-net:client-port"]

    # PE southbound to host bridges
    - endpoints: ["PE-isp:e1-2",   "net-isp:pe-isp-port"]
    - endpoints: ["PE-nomad:e1-2", "net-nomad:pe-nomad-port"]
    - endpoints: ["PE-site:e1-2",  "net-site:pe-site-port"]